import { InputSeparatorDirective } from './input-separator.directive';

describe('InputSeparatorDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSeparatorDirective();
    expect(directive).toBeTruthy();
  });
});
